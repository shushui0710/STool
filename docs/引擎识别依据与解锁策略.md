# 引擎识别依据与解锁策略

> 生成时间：2026-09-10 ｜ 对应实现：`src/engines/recognize.rs`（识别）、`src/features/unlock.rs`（解锁）
>
> 本文件是**检测层与解锁层的单一事实来源**：改识别判据或解锁策略时，**同步改这里**。
> 文档里的引擎顺序与 `UNLOCK_CATALOG` 一致，可直接对照代码。

---

## 0. 两层架构

```
① 检测层：ScanCtx（1×read_dir + 1×walkdir）──► 各插件/识别器打分 ──► Detection{score, evidence}
                                                                     │ score ≥ 60 才算"确认识别"
② 解锁层：UnlockSpec 策略表（按 engine_id 查）──► pick_route ──► 六类手段之一
```

- **检测**只用 `ScanCtx` 的内存索引，**零额外 IO**（`Engine::detect_scan` 不得自己遍历目录）。
- **解锁**只用声明式策略表 + 统一调度器；引擎私有实现走 `Engine::unlock_impl(route, ctx)`。
- 两者都以 `engine_id` 为键，新增引擎时**各改一处 + 各加一行**。

---

## 1. 检测层

### 1.1 判定线与置信度

| 分数 | `Confidence` | 含义 |
|---|---|---|
| ≥ 85 | 高置信 | 特征齐全，基本不会错 |
| 60–84 | 已确认 | `DETECT_LINE = 60`，确认识别 |
| 30–59 | 疑似（证据不足） | 会附"差 N 分未达判定线"提示 |
| < 30 | 未识别 | 不出现在结果里（0 分被过滤） |

排序键：`(score, plugin.priority)` **降序**。识别器（盲区引擎）的 `priority` 一律 < 60，
保证同分时**正式引擎优先**（已有单测 `test_recognizer_priority_below_real_engines` 守住）。

### 1.2 判据库（`recognize::Sig`）

| 判据 | 命中条件 | 数据来源 |
|---|---|---|
| `Ext(e)` | 任意位置存在 `*.e` | `ext_count` |
| `ExtAny([e…])` | 存在任一扩展名 | `ext_count` |
| `ExtN(e, n)` | `*.e` 数量 ≥ n | `ext_count` |
| `ExtSum([e…], n)` | 这些扩展名数量之和 ≥ n | `ext_count` |
| `RootFile(f)` | 根目录一级存在 `f` | `root_files` |
| `RootFileEnds(sfx)` | 根目录一级存在 `*sfx` | `root_files` |
| `RootDir(d)` | 根目录一级存在 `d/` | `root_dirs` |
| `RootDirEnds(sfx)` | 根目录一级存在 `*sfx/` | `root_dirs` |
| `Dir(d)` | 任意深度存在 `d/`（≤5 层） | `dir_names` |
| `DirEnds(sfx)` | 任意深度存在 `*sfx/` | `dir_names` |
| `FileNamed(f)` | 存在 `f`：根目录一级 **或** `NOTABLE_FILES` 白名单（任意深度） | `root_files` + `notable_files` |
| `ExeContains(sub)` | 根级 exe 名含 `sub` | `exes` |
| `ExeAnyContains([sub…])` | 根级 exe 名含任一 `sub` | `exes` |
| `NumberedVol` | 存在"分卷"（扩展名为 3 位数字，如 `x.pfs.000`） | `ext_count` |

> `NOTABLE_FILES`（`engines/scan.rs`）是"埋起来的强特征"白名单，匹配全名（大小写不敏感）：
> `actors.json`、`assembly-csharp.dll`、`gameassembly.dll`、`project.godot`、`config.tjs`、
> `startup.tjs`、`data.win`、`gameexe.dat`、`gameexe.ini`、`seen.txt`、`tyrano.js`、`main.lua`、
> `bregexp.dll`、`bgi.gdb`、`libcocos2d.dll`、`rpg_rt.exe`、`nscript.dat`、`arc.nsa`、`system40.exe`。
> **新增白名单条目 = 扩大 `FileNamed` 的表达力**，改 `scan.rs` 一处即可。

### 1.3 打分原则

- **强特征单独过线**：专有文件名 / 扩展名（`.mjo`、`.ypf`、`data.win`、`BREGEXP.DLL`）给 ≥ 60 分。
- **弱特征必须组合**：`.arc`、`.pak`、`.dat`、`.pfs` 这类"谁都在用"的，单条给 20–45 分，靠组合过线。
- **消歧**：`.pfs` 同名于 BGI 与 Artemis —— **原生 `artemis` 插件优先级更高（70）**，
  以「`.pfs` 真实可解析（`pf6`/`pf8` 头 + 索引校验通过）+ `.asb`/`.ast` 伴随文件」为准；
  `.pfs` 单条 30 分只作盲区识别器里的弱证据，靠组合过线。
- **升级为原生插件后要清理旧识别器**：`artemis` 从"盲区识别器"升级为原生插件（真解包/回封）时，
  必须删掉 `recognize.rs` 里的同名条目与该条目的测试断言，否则 id 冲突 / 双份打分。

---

## 2. 解锁层

### 2.1 六类手段（`UnlockRoute`）

| key | 手段 | 自动 | 说明 |
|---|---|---|---|
| `bundled` | 替换自带全CG存档 | ✅ | 把游戏目录里自带的「全CG存档」按同名复制进存档目录。**与引擎无关**，最省事、成功率最高 |
| `ingame` | 游戏内全开开关 | ❌ | 设置 / 隐藏界面的 toggle，只能提示用户去找 |
| `config` | 改配置文件开关 | ✅ | json / ini / xml / txt 里的解锁开关 |
| `registry` | 改注册表键位 | ✅ | Unity PlayerPrefs `<键>_h<djb2 哈希>=1` |
| `savefile` | 改存档位 / 计数 | ✅ | 二进制存档、pickle `persistent` 里的位标志 |
| `manual` | 人工分析 | ❌ | Cheat Engine / 内存 hook |

### 2.2 路线选择（`pick_route`）

优先级从高到低：

1. **`--opt:route=<key>` 显式指定**（写错 key 直接报错并列出合法值，不静默回退）；
2. **带 `--opt:restore=`** → 用该引擎的首选（引擎私有）路线（目前仅 Unity 支持还原）；
3. **发现「自带全CG存档」** → `bundled`（覆盖引擎自身偏好：能"复制存档"就别"改存档"）；
4. **否则** → 策略表第一条（引擎首选）。

### 2.3 安全设计（这是功能能用的前提）

| 机制 | 实现 |
|---|---|
| **默认只读** | 不带 `--opt:apply=1` 只预览，**不写任何文件 / 注册表** |
| **备份不可覆盖** | 覆盖任何文件前 `settings::backup_once()`（已存在即返回，绝不覆盖），可用 `stool restore` 还原 |
| **来源≠目标** | 「自带全CG存档」目录被排除出候选**目标**目录（否则会"复制到自身"） |
| **不猜关键路径** | 找不到存档目录时**拒绝落地**并提示 `--opt:save_dir=`，而不是把存档丢进游戏根 |
| **诚实告知** | 引擎无内置实现的路线直接报"没有内置自动实现 + 该怎么做"，不假称成功 |
| **注册表侧** | 哈希自检（用现存键反推）、非 `REG_DWORD` 跳过、裸值跳过、写后读回校验（见 `gallery.rs`） |

### 2.4 Unity 注册表路线的「键名从哪来」

注册表路线要写 `<原始键名>_h<djb2>`，**难点全在"键名是什么"**。本工具分两级来源：

| 级别 | 来源 | 实现 | 适用 |
|---|---|---|---|
| **精确** | Mono 版：`*_Data/Managed/Assembly-CSharp*.dll` 的 `#US` 堆 | `formats::dotnet`（PE → CLI 头 → 元数据根 → 流头，读 `#US`/`#Strings`） | Mono 版 Unity |
| **精确** | IL2CPP 版：`*_Data/il2cpp_data/Metadata/global-metadata.dat` | `formats::il2cpp`（magic `0xFAB11BAF` + v24..31 + 三区首尾相接校验） | IL2CPP 版（**无 C# 程序集**） |
| **兜底** | `<*_Data>/level*`、`sharedassets*`、`resources.assets`、`globalgamemanagers` 里的 ASCII 串 | `scan_scene_strings`（`looks_like_key` 过滤） | 两者都读不到时 |

**都不需要反编译器 / .NET 运行时**：`PlayerPrefs.SetInt("cg_flag_01", 1)` 里的字面量就躺在元数据字符串堆里，
直接读即可。实机效果（Yakuzarogue / IL2CPP v31）：直接列出真实键 `cg_button_name1`…`cg_button_name7`。

**噪声处理**（IL2CPP 字面量池里绝大多数是引擎/框架串）：

- `looks_like_key` 白名单字符集 `[A-Za-z0-9_ .-]` + 非 ASCII；首末字符必须是字母/数字/`_`；
  禁连续空格；剔除 base64 常量块（protobuf 描述符等长串）；
- `sort_candidates` 按"像不像画廊键"分层（`cg_`/`CG01`/含 gallery 关键词 → 有下划线 → 其它），
  **先排序再截断**，避免真键被字典序挤出 `max` 之外；
- 只读入口 `stool cg-candidates <游戏目录>` **完全不碰注册表**，可反复查看；写入时才走
  `unlock --opt:apply=1`（写前快照、写后读回校验、`--opt:restore=` 还原）。

### 2.5 CLI / GUI

```bash
# 只读预览（缺省）：列出该引擎可用手段 + 自带存档候选 + 存档目录
stool unlock "D:/Games/某游戏"

# 落地（自动选路线：有自带存档优先用它）
stool unlock "D:/Games/某游戏" --opt:apply=1

# 指定手段 / 指定目标目录
stool unlock "D:/Games/某游戏" --opt:apply=1 --opt:route=registry --opt:filter=CG
stool unlock "D:/Games/某游戏" --opt:apply=1 --opt:route=bundled --opt:save_dir="D:/Games/某游戏/SaveData"

# 查能力矩阵 / 查解锁策略表
stool engines            # 每个插件的 能力 + 识别依据 + 解锁动作 + 手段优先序
stool unlock-support     # 全部引擎的解锁策略表
```

GUI：资源解包页的「解锁辅助」按钮 → 「全 CG 解锁」面板（手段下拉 + 只读预览开关 + 键名过滤）。

---

## 3. 引擎总表

> 「识别依据」= 检测判据；「解锁手段」按优先序（`>` 表示"优先于"）；
> 「存档目录」为策略表提示，实际会与"名字像存档目录"的根级目录合并。

### 3.1 原生插件（可解包 / 可处理）

| 引擎 | 插件 id | 识别依据（关键判据） | 解锁手段 | 存档目录 | 外部工具 |
|---|---|---|---|---|---|
| Ren'Py | `renpy` | `renpy/` + `game/` 目录、`.rpa`、`.rpyc` | `savefile` > `bundled` > `ingame` | `game/saves`、`%AppData%/RenPy/<名>` | unrpyc |
| RPG Maker MV/MZ | `rpgmaker_mv` | `data/Actors.json`、`js/rpg_core.js`、`game.rpgproject` | `bundled` > `config` > `ingame` | `www/save`、`save` | —— |
| RPG Maker XP/VX/Ace | `rpgmaker_rgss` | `Data/Scripts.rvdata2`、`Game.ini`、`.rgssad/.rgss2a/.rgss3a` | `bundled` > `savefile` > `config` | `Save` | —— |
| KiriKiri | `kirikiri` | `*.xp3`、`config.tjs`、`startup.tjs`、`.ks/.tjs` | `bundled` > `config` > `ingame` | `savedata` | GARbro |
| Godot | `godot` | `project.godot`、`*.pck` | `bundled` > `config` | `%AppData%/Godot/app_userdata/<名>` | GDRE Tools |
| NScripter | `nscripter` | `nscript.dat`、`arc.nsa` | `bundled` > `config` > `ingame` | `savedata` | GARbro |
| TyranoBuilder | `tyrano` | `tyrano/tyrano.js`、`.tjs`、`.ks` | `bundled` > `config` | `savedata` | —— |
| HTML / Electron | `html_game` | `index.html` + `js/`、`resources/app.asar` | `bundled` > `config` > `ingame` | `resources/app`、`save` | —— |
| Artemis Engine | `artemis` | **`.pfs` 可解析为 pf6/pf8**（70）、`.pfs2`（45）、`.asb`（25）、`.ast`（15）、分卷（45，仅疑似） | `bundled` > `config` | —— | —— |
| Wolf RPG | `wolf` | `*.wolf`、`Data/BasicData` | `bundled` > `savefile` | `SaveData` | WolfDec |
| Unity | `unity` | `UnityPlayer.dll`、`*_Data/`、`app.info`、`Assembly-CSharp.dll`/`GameAssembly.dll` | `registry` > `bundled` > `ingame` | `SaveData` | AssetRipper、Il2CppDumper |
### 3.2 盲区识别器（识别 + GARbro 解包 + 解锁指引）

| 引擎 | 插件 id | 识别依据（分值） | 解锁手段 |
|---|---|---|---|
| Ethornell / BGI | `bgi_ethornell` | exe 含 `bgi`/`ethornell`/`エンジン設定`(60)、`BREGEXP.DLL`(60)、`bgi.gdb`(55)、`.arc`(40)、`GameData/`(30) | `bundled` > `savefile` > `config` > `ingame` |
| SiglusEngine | `siglus` | `Gameexe.dat`(55)、`.pak`(30)、`.g00`(15) | `bundled` > `config` |
| RealLive | `reallive` | `Gameexe.ini`(50)、`.g00`(30)、`seen.txt`(25) | `bundled` > `config` |
| Majiro | `majiro` | `.mjo`(65)、exe 含 `majiro`(40)、`.arc`(20) | `bundled` > `config` |
| YU-RIS | `yuris` | `.ypf`(65)、`.ybn`(20)、`.yui`(15) | `bundled` > `config` |
| CatSystem2 | `catsystem2` | `.int`(45)、`.cst`(25)、`.noa`(15) | `bundled` > `config` |
| AliceSoft | `alicesoft` | `.ald`(65)、`.afa`(40)、`.alk`(20)、`system40.exe` | `bundled` > `savefile` |
| LiveMaker | `livemaker` | `.prt`(55)、`.grp`(30)、`.grs`(15) | `bundled` > `config` |
| Nitroplus | `nitroplus` | `.npk`/`.npa`(65)、`.scr`(20) | `bundled` > `config` |
| RPG Maker 2000/2003 | `rpgmaker2k` | `.ldb`(60)、`.lmt`(30)、exe 含 `rpg_rt`(30) | `bundled` > `savefile` |
| Unreal Engine | `unreal` | `.utoc`/`.ucas`(65)、`.uasset`(45)、`Engine/`(45)、`Binaries/`(30) | `bundled` > `savefile` > `ingame` |
| LÖVE | `love2d` | `.love`(65)、`main.lua`(30)、`.lua`(15) | `bundled` > `config` |
| GameMaker | `gamemaker` | `data.win`(65) | `bundled` > `config` |
| Purple | `purple` | `.lpk`(60) | `bundled` > `config` |
| **Cocos2d-x / JSB**（本轮新增） | `cocos2dx` | `libcocos2d.dll`(65)、`Resources/`(20)、`script/`(15) | `bundled` > `config` |
| **Flash / SWF**（本轮新增） | `flash` | `.swf`(60)、exe 含 `flash`/`adobe`(20) | `bundled` > `config` |
| **QSP**（本轮新增） | `qsp` | `.qsps`/`.qsp`(60)、exe 含 `qsp`(20) | `bundled` > `config` |
| 未知（兜底） | `generic` | 无特征命中时的占位 | `bundled` > `ingame` > `config` > `savefile` > `manual` |

---

## 4. 本轮改动清单

| 项 | 内容 |
|---|---|
| 新增 `features/unlock.rs` | `UnlockRoute`（6 类）、`UnlockSpec`、`UNLOCK_CATALOG`（28 引擎）、`find_save_dirs`、`find_bundled_saves`、`pick_route`、`preview_msg`、`run`、`bundled_route` |
| `Engine` trait | 新增 `unlock_routes()`（默认查表插值兜底）与 `unlock_impl(route, ctx)`；`unlock()` 默认实现改为走统一调度器 |
| Unity / Ren'Py | 由"各自实现 `unlock`"改为"实现 `unlock_impl`"，自动获得统一预览与路线分派 |
| `effective_capabilities()` | 能力 = 插件声明 ∪ 策略表推导（凡有解锁路线的引擎自动具备 `Op::Unlock`），GUI/CLI 统一取用 |
| `Sig` 扩充 | 新增 `ExtSum` / `RootFileEnds` / `RootDir` / `DirEnds` / `FileNamed` / `ExeAnyContains` / `NumberedVol` |
| `scan.rs` | 新增 `has_dir_ending_with` / `has_numbered_volume`；`NOTABLE_FILES` 白名单 +7 条 |
| 识别器强化 | `bgi_ethornell`（BREGEXP.DLL / bgi.gdb / 日文 exe）、`artemis`（分卷消歧）、`unreal`（目录布局） |
| 识别器新增 | `cocos2dx`、`flash`、`qsp` |
| CLI | 新增 `engines` / `unlock-support`；`detect` 输出"选定引擎"；`unlock` 支持 `--opt:route=` / `--opt:save_dir=` |
| GUI | 解锁面板泛化为任意引擎（手段下拉 + 只读预览）；资源页显示 `Engine::describe` 引擎详情 |
| 死代码复活 | `describe`（GUI 引擎详情，后台算好）、`capability_matrix`（`engines` 命令）、`pick`（改为 `pick_from` 供 CLI/GUI 复用，避免重复扫描） |

### 4.1 后续补充（Artemis 原生支持 / Unity 精确键名）

| 项 | 内容 |
|---|---|
| `artemis` 升级为原生插件 | 删除 `recognize.rs` 里的旧盲区条目（避免 id 冲突），新增 `engines/artemis.rs`（priority 70）：`.pfs` 解包（并行 + 断点续传）、按原索引顺序回封（沿用原代次）、分卷明确报错；**`.pfs` 用真实可解析性作判据**，而非只靠后缀 |
| 新增 `formats/pfs.rs` | pf6 / pf8 索引解析 + 写入；pf8 的 XOR 密钥流**每条目从 `key[0]` 重新开始**（与 GARbro 按全局偏移递进不同，见模块内注释与真机验证）；`hash.rs` 补流式 SHA-1 |
| 新增 `formats/dotnet.rs` | 最小 .NET/PE 元数据读取（`#US` / `#Strings`），零依赖、不反编译、不执行 |
| 新增 `formats/il2cpp.rs` | `global-metadata.dat` 字符串读取（v24..31），带**布局自检**（三区首尾相接 + 字面量 UTF-8 可解率 ≥90%） |
| `gallery.rs` 键名两级来源 | `scan_precise`（程序集/IL2CPP 精确）+ `scan_scene_strings`（场景启发式兜底）；`looks_like_key` 加白名单字符集与 base64 常量块剔除；`sort_candidates` 先排序后截断 |
| 新增 CLI `cg-candidates` | **完全不碰注册表**的只读候选查看入口，便于写入前确认与排查"为什么一个键都没扫到" |
| `selfcheck.rs` | `Kind` 增加 `Pfs8` / `Pfs6`，Artemis 封包纳入"解包→重打包→逐条目比对"闭环 |

---

## 5. 新增一个引擎要改哪几处（checklist）

1. **`engines/scan.rs`**：若强特征埋在子目录，把文件名加进 `NOTABLE_FILES`。
2. **`engines/recognize.rs`**（盲区引擎）或 **`engines/xxx.rs`**（原生插件）：
   加识别规则 / `detect_scan`，`priority` 记得低于同族正式引擎。
3. **`features/unlock.rs`**：在 `UNLOCK_CATALOG` 加一条 `UnlockSpec`（`routes` / `save_dirs` / `basis` / `action`）。
   —— 只加这一条，`Op::Unlock` 与 GUI 面板会自动生效，**不必改 `capabilities()`**。
4. **有私有解锁实现时**：实现 `Engine::unlock_impl(route, ctx)`（如 Unity 的 `Registry`）。
5. **本文件**：补一行引擎总表 + 必要的"消歧"说明。
6. **测试**：识别加一条 `detect` 断言；解锁加一条"自带存档 / 路线选择"断言。

---

## 6. 已知局限

- **Unity 注册表路线的候选键名**：Mono 版走 `Assembly-CSharp*.dll` 的 `#US` 堆、IL2CPP 版走
  `global-metadata.dat` 的字面量池，**都是精确字符串**；但 IL2CPP 池里引擎/框架串占绝大多数
  （实测 5000–9000 条），故仍可能混入无关名字。已用字符集白名单 + base64 常量块剔除 + 相关性排序
  缓解（见 §2.4），工具会报出"高置信候选"条数；**最稳的仍是优先找游戏自带的「全开开关」**。
  若某游戏的字面量被运行时解密（如 Beebyte 之类混淆），元数据里读不到真键 —— 此时会明确退回
  启发式路径并如实说明，而不是假装成功。
- **IL2CPP 元数据只支持 v24..31**：区间外（或布局校验不通过）直接退回启发式，不去猜偏移。
- **`bundled` 路线依赖"游戏跑过一次"**：存档目录通常是运行时生成的；没生成时需显式 `--opt:save_dir=`。
- **`config` / `savefile` 对多数盲区引擎只有指引**：这些引擎的存档格式未公开，自动改动风险高，故只做诚实提示。
- **`manual`**（Cheat Engine / 内存 hook）不自动化：越界且不可靠。
- **分卷检测是"宽松"的**：任何 3 位数字扩展名都会命中 `NumberedVol`，故它只给 45 分（必须与 `.pfs` 组合）；
  Artemis 分卷封包（`x.pfs.000`）本工具**不支持拼接**，只识别并给替代做法。
